{
  "name": "dashboard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "echo 'dashboard: no sources at this stage' && exit 0",
    "test": "echo 'dashboard: no tests at this stage' && exit 0",
    "test:e2e": "echo 'dashboard: no e2e at this stage' && exit 0"
  }
}
